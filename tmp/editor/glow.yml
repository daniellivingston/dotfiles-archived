# ==== overriding default config yaml ======================================================== #
# GLOW_CONFIG=`glow config --help | tail -1 | cut -d "(" -f2 | cut -d ")" -f1 | cut -d' ' -f2`
# ln -s "${HOME}/dev/dotfiles/editor/glow.yml" "${GLOW_CONFIG}"
# ============================================================================================ #
#
# style name or JSON path (default "auto")
style: "light"
# show local files only; no network (TUI-mode only)
local: true
# mouse support (TUI-mode only)
mouse: true
# use pager to display markdown
pager: true
# word-wrap at width
width: 80

